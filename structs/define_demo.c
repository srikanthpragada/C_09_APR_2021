#define SIZE 50
// Macro
#define iseven(n) n % 2 == 0

// Enumeration
enum paymentmodes  {CASH = 1, CC = 2, BANK = 3};


void main()
{
 int a[SIZE],i;
 enum paymentmodes paymentmode;


     for(i = 0; i < SIZE; i ++)
        ;


     for(i = 0; i < SIZE; i ++)
        ;

     if(iseven(i))  // if(i % 2 == 0)
          ;

     paymentmode = CASH; // cash

     if(paymentmode == BANK)
         ;

}
